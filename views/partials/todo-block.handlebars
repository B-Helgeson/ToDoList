<li>
	{{name}}

	<button class="change-completed" data-id="{{id}}" data-newtodo="{{todo}}">
		{{#if done}}Finish!{{else}}Undo!{{/if}}
	</button>

	<button class="delete-todo" data-id="{{this.id}}">DELETE!</button>
</li>